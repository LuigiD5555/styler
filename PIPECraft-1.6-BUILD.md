# Temporary PipeCraft 1.6 build branch

This branch exists only to build and verify the `1.6.0-alpha.1` structured-spec IPC runtime used by Styler 0.14 development. It is not intended to merge into `main`.
